

<div class= "row">
<br>
  <center><h1> Movies Opening this Week </h1></center>
  <section class="small-6 small-centered columns">
      <table>
      <thead>
          <tr>
            <th>Movie-Id </th>  
            <th>Movie</th>
            <th>Year</th>
          </tr>
      </thead>
        <tbody>
  
        <% @list3.each do | movie | %>
          <tr>          
             <td><%= movie.imdb_id %></td>
             <td><a href="http://www.imdb.com/title/tt<%= movie.imdb_id %>/?ref_=inth_ov_tt"><%= movie.title %></a></td>
             <td><%= movie.year %> </td>
          </tr>
         <% end %>
  
  
        </tbody>
      </table>
  </section>
</div>
